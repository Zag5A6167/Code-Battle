 const handleJoinTeam = (teamName) => {
        if (socket && roomId) {
            socket.emit('joinTeam', roomId, socket.id, teamName);
        }
    };


    const handleStartGame = () => {
        if (socket && isHost) {
            socket.emit('startGame', roomId);
        }
    };
